# -------------------------
# 1) 기본: 전부 무시 (빌드 컨텍스트 최소화)
# -------------------------
**

# -------------------------
# 2) Docker 빌드에 필요한 파일만 포함(Allowlist)
# -------------------------
!Dockerfile
!.dockerignore
!pyproject.toml
!uv.lock
!.python-version

# 앱 소스
!app/
!app/**

# -------------------------
# 3) app 아래라도 불필요한 아티팩트는 제외
# -------------------------
app/**/__pycache__/
app/**/*.py[cod]

app/**/.pytest_cache/
app/**/.mypy_cache/
app/**/.ruff_cache/
app/**/.pytype/

app/**/.coverage
app/**/htmlcov/
app/**/*.log

# OS/에디터 잡동사니(혹시 app 아래에 생겨도 제외)
app/**/.DS_Store
app/**/.env
app/**/.env.*

# -------------------------
# 4) (중요) 모델 weight/대용량 파일은 기본 제외
#    나중에 Step3에서 모델을 이미지에 포함할지, S3/EFS로 둘지 결정 가능
# -------------------------
app/ai_models/**/*.pt
app/ai_models/**/*.pth
app/ai_models/**/*.ckpt
app/ai_models/**/*.onnx
scr/
validation_pkg/
