[project]
name = "splice-playground-dev-gui"
version = "0.1.0"
description = "Isolated Streamlit utility for STEP3/STEP4 backend verification"
requires-python = ">=3.12,<3.13"
dependencies = [
    "matplotlib>=3.10,<4",
    "numpy>=2,<3",
    "streamlit>=1.52,<2",
]

[tool.uv]
package = false
